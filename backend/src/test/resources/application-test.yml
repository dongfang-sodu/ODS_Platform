spring:
  datasource:
    url: jdbc:h2:mem:ods-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
ods:
  seed:
    enabled: false
  security:
    jwt-secret: test-secret-that-is-long-enough-for-hmac-sha-signing
